# Tgemm
gemm template.

## How to use
to practice sgemm
```sh
mkdir build && cd build
cmake -DS=ON .. # Sgemm is default on
```

to practice hgemm
```sh
mkdir build && cd build
cmake -DH=ON .. # Hgemm is default off
```
